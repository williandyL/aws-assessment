NB: To finish this task take me 4 hours. I need to explore more about AWS congnito, ECS and github action.

To run terraform scipt:
terraform init
terraform apply


To run script just run the pipeline
But if you want to manually run the pipeline add this env

# CLIENT_ID="YOUR_COGNITO_CLIENT_ID"
# USERNAME="testuser@example.com"
# PASSWORD="YourPassword123!"


# API_R1="https://r1-api.execute-api.us-east-1.amazonaws.com/prod"
# API_R2="https://r2-api.execute-api.eu-west-1.amazonaws.com/prod"

Before run the scipt



I am deploying api-gateway, ecs, lambda, vpc in 2 region.y
For dynamodb I am using replica for data synchronization.
